import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krashout.app',
  appName: 'krash-out',
  webDir: 'dist/krash-out/browser'
};

export default config;
